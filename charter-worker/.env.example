# charter-worker — Variables de entorno
# Copia este archivo a .env y completa los valores

# API key para el LLM (agno / OpenAI / etc.)
OPENAI_API_KEY=sk-...

# Modelo a usar (opcional, default: gpt-4o)
MODEL_NAME=gpt-4o

# Puerto del servidor (default: 8000)
PORT=8000
